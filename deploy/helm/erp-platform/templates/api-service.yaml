apiVersion: v1
kind: Service
metadata:
  name: {{ include "erp-platform.fullname" . }}-api
  labels:
    {{- include "erp-platform.api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.api.service.type }}
  ports:
    - port: {{ .Values.api.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "erp-platform.api.selectorLabels" . | nindent 4 }}
