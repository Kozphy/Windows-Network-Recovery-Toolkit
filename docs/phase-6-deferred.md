# Phase 6 — Deferred scope

Items intentionally deferred until the Phase 1–4 vertical slice is stable in CI:

- Multi-agent LLM orchestration with retry policies
- Root cause LLM with explanation guardrails only (no execution)
- Risk heatmap and control coverage matrix UI
- ETW live subscription (stub remains in `evidence/etw_reader.py`)
- MCP write tools (ingest preview, review queue actions)
- Hypothesis generation engine as separate service

Track via [production-readiness-gap.md](production-readiness-gap.md).
