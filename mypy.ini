[mypy]
exclude = ^src/platform_core/analytics/powerbi_star_export\.py$

# The legacy star exporter has one nullable timestamp assignment that predates
# the outcome-intelligence PR. Keep the broader portfolio type gate strict while
# isolating that file until its dedicated schema-typing cleanup.
